{
  "$name": "English",
  "text_current_lowercase": "current",
  "text_title": "AutoJs6 Plugin: YOLO NCNN",
  "text_plugin_synopsis": "Process-isolated, fully offline YOLO object detection for AutoJs6 (NCNN {{ ncnn_version }} backend)",
  "h3_languages": "Languages",
  "p_languages": "The current README.md supports the following languages",
  "h3_introduction": "Introduction",
  "p_introduction": "The AutoJs6 YOLO NCNN plugin lets AutoJs6 scripts run YOLO object detection entirely on-device: pass in an image and get back an array of detections with labels, confidences, and pixel-coordinate bounding boxes. Inference is performed by Tencent NCNN {{ ncnn_version }} in a separate `{{ runtime_process }}` process, with no network access and no data upload; model files are supplied by the user and the plugin APK ships no models.",
  "p_identity_note": "The identity above is what the host uses to discover and bind this plugin. Models are handed over by the host through read-only file descriptors and always remain external resources.",
  "h3_features": "Features",
  "features": [
    "Offline YOLO11 object detection: feed an AutoJs6 `images` module image object, get labels + confidences + bounding boxes, computed entirely on-device.",
    "Process isolation: inference runs in the separate `{{ runtime_process }}` process, so native-layer failures never take down the AutoJs6 main process; services are protected by the `org.autojs.permission.PLUGIN` permission and signature checks.",
    "NCNN {{ ncnn_version }} CPU inference backend: adjustable thread count (default {{ default_cpu_threads }}, up to {{ max_cpu_threads }}), for `arm64-v8a` devices.",
    "Manifest-driven model compatibility: `{{ model_manifest_file }}` declares inputs, outputs, and labels, supporting 1 to {{ max_labels }} custom classes; official YOLO11 and self-trained models work alike.",
    "Model safety validation: session open verifies the declared length and SHA-256 of all three model files, and the actual NCNN graph output shape is verified at runtime; mismatches are rejected, never guessed at.",
    "Stable error categories: missing component, provider unavailable, model rejected, unsupported capability, and similar cases all surface decidable error codes that scripts can handle precisely.",
    "Controlled timeouts and lifecycle: both model open and each detection have timeout ceilings; `detector.close()` and stopping the script release the session and native resources immediately.",
    "README and CHANGELOG are available in ten languages: Simplified Chinese, Traditional Chinese (HK/TW), English, French, Spanish, Japanese, Korean, Russian, and Arabic."
  ],
  "h3_quick_start": "Quick Start",
  "quick_start_install_title": "Install",
  "quick_start_install": "This plugin is currently in a private staging phase (see the Project Status section below): public downloads and the official plugin index entry arrive only after the compatible host AutoJs6 {{ required_host_version }} (build {{ required_host_build }}) is formally released. Until then you can build a TEST-SIGNED candidate yourself as described in the Build section and pair it with an AutoJs6 test APK using the same debug certificate; host and plugin must be signed with the same certificate.",
  "quick_start_enable_title": "Enable",
  "quick_start_enable": "Installing the plugin does not switch YOLO on by itself: the AutoJs6 host keeps explicit selection, trust, and enable switches (the YOLO route is off by default), so enable and trust this provider in the host. On the script side, `yolo.load` also requires the explicit component string in `options.component`; there is no implicit fallback.",
  "quick_start_run_title": "Run",
  "quick_start_run": "Prepare a model directory containing the three files `{{ model_manifest_file }}`, `{{ model_param_file }}`, and `{{ model_bin_file }}` (see the Model Preparation section below), open a detector with `yolo.load(modelDir, options)`, get the detection array with `detector.detect(image, options)`, and release it with `detector.close()` when done.",
  "quick_start_debug_title": "Troubleshoot",
  "quick_start_debug": "Exceptions thrown by `yolo.load` and `detector.detect` carry stable error categories: `COMPONENT_REQUIRED` (no component given), `PROVIDER_UNAVAILABLE` (host cannot find or does not trust the provider), `MODEL_REJECTED` (model or manifest failed validation; details carry prefixes such as `MANIFEST_*`), `UNSUPPORTED_CAPABILITY` (a capability outside CPU/arm64/detect was requested), `SESSION_CLOSED`, `DETECT_FAILED`, and so on. Check the Boundaries section below and the [model manifest specification]({{ model_manifest_doc_url }}) when debugging.",
  "h3_usage": "Usage Example",
  "p_usage_intro": "A minimal ready-to-run example (see also `sample/yolo/detect.js` in the host repository)",
  "p_usage_note": "Each detection carries `classId`, `label`, `confidence`, and `bounds` (an Android `RectF` with `left` / `top` / `right` / `bottom` fields plus `centerX()` / `centerY()` methods); coordinates are in input-image pixels. A detector is a single-request serial session: the inference queue length is 0, so a second concurrent `detect` on the same detector fails immediately instead of queueing.",
  "h3_models": "Model Preparation",
  "p_models_intro": "A model directory always contains exactly three files, and file names select roles",
  "p_models_export": "Official or self-trained Ultralytics YOLO11 detect models can be exported with `yolo export format=ncnn imgsz={{ input_size }}`, which produces `{{ model_param_file }}` and `{{ model_bin_file }}` (see the [Ultralytics NCNN export guide]({{ ultralytics_ncnn_url }})). `{{ model_manifest_file }}` is a Model Manifest v1 document: it declares the input (`in0`, RGB NCHW, {{ input_size }}x{{ input_size }} letterbox), the output (`out0`, `{{ decoder_id }}` decoder, shape `[1, 4 + N, 8400]` where N is the class count), and the label list; a complete example is [fixtures/yolo11n/model-manifest-v1.json]({{ manifest_example_url }}).",
  "p_models_note": "The manifest is a compatibility contract, not a relabeling tool: session open verifies the declared lengths and SHA-256 of all three files, and the actual NCNN graph output shape is verified at runtime; mismatches are rejected with `MODEL_REJECTED` (detail prefixes such as `MANIFEST_SHAPE_INVALID`, `MODEL_GRAPH_REJECTED`). Models keep the license and usage conditions of their source; converting to NCNN does not change them, and the plugin grants no redistribution rights. See the [model manifest specification]({{ model_manifest_doc_url }}) and the [model license policy]({{ model_license_doc_url }}).",
  "h3_script_api": "Script API",
  "p_api_load": "`yolo.load(modelDir, options)` opens a detection session and returns a `YoloDetector`. `options.component` is required (a package/class component string; for this plugin it is `{{ provider_component }}`). Optional: `device` (only `\"cpu\"` is accepted currently), `threads` (default {{ default_cpu_threads }}, up to {{ max_cpu_threads }}), `decoderId` (default `{{ decoder_id }}`), and `timeoutMillis` (total model-open timeout, default {{ default_open_timeout_millis }} ms, up to {{ max_timeout_millis }} ms).",
  "p_api_detect": "`detector.detect(image, options)` runs detection synchronously on one image and returns the detection array. `image` is an AutoJs6 `images` module image object (from `images.read`, a screenshot, etc.). Optional: `confidence` (confidence threshold, default {{ default_confidence }}), `iouThreshold` (NMS IoU threshold, default {{ default_iou }}), `maxDetections` (default {{ default_max_detections }}, up to {{ max_detections }}), and `timeoutMillis` (default {{ default_detect_timeout_millis }} ms).",
  "p_api_close": "`detector.close()` releases the session and native resources and is safe to call repeatedly; AutoJs6 also closes detectors when the script ends, but an explicit `try...finally` release is recommended. Calling `detect` after close returns `SESSION_CLOSED`.",
  "h3_limits": "Boundaries",
  "p_limits_intro": "To keep behavior predictable, requests outside the following scope are rejected explicitly rather than silently falling back",
  "limits": [
    "CPU inference only: Vulkan/GPU is unsupported and `options.device` accepts only `\"cpu\"`.",
    "`arm64-v8a` only: devices with other ABIs cannot load this plugin's native library.",
    "Object detection (detect) only: segmentation, pose, OBB, classification, and tracking are all unsupported.",
    "Only the `{{ decoder_id }}` decoder is registered: an unknown `decoderId` is rejected instead of falling back to another decoder.",
    "Input is preprocessed as a {{ input_size }}x{{ input_size }} letterbox (the fixed manifest v1 profile) with RGBA_8888 pixels.",
    "Single-request serial inference per session: the queue limit is 0, so a second concurrent `detect` on the same session fails.",
    "Model open accepts read-only descriptors of regular files only (no pipes or sockets); all three files must be readable.",
    "Installing this plugin does not enable YOLO by itself: enable, trust, and selection state always belong to the AutoJs6 host."
  ],
  "h3_security": "Security & Isolation",
  "p_security_intro": "The plugin is designed fail-closed; the following mechanisms are always in effect",
  "security_limits": [
    "Inference runs in the separate `{{ runtime_process }}` process, isolated from the AutoJs6 main process; services are protected by the `org.autojs.permission.PLUGIN` permission and signature checks.",
    "Models arrive from the host as read-only `ParcelFileDescriptor` instances; the plugin reads no storage on its own and makes no network requests.",
    "Before a session opens, declared lengths, EOF, and SHA-256 are verified; the whole open shares one monotonic deadline, and an expired session is never published.",
    "If the NCNN runtime cannot be loaded or initialized, the plugin fails closed instead of degrading.",
    "Malformed ingress is isolated from active requests; when a request identity can be recovered, an exact failure terminal is published instead of hanging.",
    "Callback death and stale sessions are detected and cleaned up; native resources are released deferred and close operations are idempotent."
  ],
  "h3_compatibility": "Compatibility",
  "p_compatibility": "Requires AutoJs6 with a version code of at least {{ required_host_build }} (that is, {{ required_host_version }} or later) signed with the same certificate as the plugin; Android {{ android_min_sdk }}+ (Android 7.0), targetSdk {{ android_target_sdk }}; the device must be `{{ supported_abi }}`. Plugin protocol version {{ protocol_version }}; current provider version {{ version_name }} (version code {{ version_build }}).",
  "h3_project_status": "Project Status",
  "p_project_status": "This repository is currently a private staging archive: the compatible host AutoJs6 {{ required_host_version }} ({{ required_host_build }}) has not been formally released, and this plugin is neither publicly released nor listed in the official plugin index; the GitHub badges above may not render until the repository goes public. Without `sign.properties`, `assembleRelease` produces an unsigned APK that is source/build evidence only, not a publishable artifact. The first release is {{ version_name }} (version code {{ version_build }}, with no version code 1 predecessor); defects are fixed forward as version code 3, never by rollback. Production signing, final on-device verification, and publication status are bound by the external R6 evidence archive; see the [engineering notes]({{ engineering_notes_url }}).",
  "h3_build": "Build",
  "p_build_intro": "JDK 21+ is recommended; the Android SDK must provide platforms {{ android_min_sdk }} and {{ android_target_sdk }}, plus NDK {{ ndk_version }} and CMake {{ cmake_version }} (needed to compile the NCNN JNI). Common commands",
  "p_build_variants": "`assembleRc` produces an installable arm64-only TEST-SIGNED candidate: it inherits the release R8 and resource shrinking, uses the standard debug signing, and its version name ends in `-rc-test-signed`; it is meant to pair with an AutoJs6 test APK using the same certificate for on-device verification. `assembleRelease` is the release build and stays unsigned when signing material is absent.",
  "p_build_gate": "The maintainer gate `tools/verify-r6-provider-source.ps1` first runs `--check` over all 22 generated README/CHANGELOG artifacts for 10 languages and fails immediately on drift. It then starts from clean sources by default: after `:app:clean` it runs the focused tests and both APK assemblies, records test XML and artifact hashes, and verifies the APK against a five-file asset allowlist. Local verification and doc generation both run offline by default (zero network calls) to stay clear of Cloudflare 502/524/529 noise on the development network.",
  "h3_release_history": "Release History",
  "h5_more_release_history": "For more release history, see",
  "h3_license": "License",
  "p_license": "The plugin source is released under [MPL-2.0]({{ license_url }}). The APK bundles the complete NCNN license and notices (BSD-3-Clause plus its upstream third-party notices), the full Apache-2.0 text for the Kotlin runtime, and the third-party notice index; see [THIRD_PARTY_NOTICES]({{ third_party_notices_url }}). Models are external resources: they keep the license and usage conditions of their source, and the plugin ships no models and grants no redistribution rights.",
  "h3_further_reading": "Further Reading",
  "further_reading": [
    "[Model manifest specification (Model Manifest v1)]({{ model_manifest_doc_url }})",
    "[Model manifest JSON Schema]({{ manifest_schema_url }})",
    "[Model and validation-asset license policy]({{ model_license_doc_url }})",
    "[0.1.0 release notes (engineering wording)]({{ release_notes_url }})",
    "[Engineering notes (the former audit-style README in full)]({{ engineering_notes_url }})",
    "[Project roadmap (history milestones and future plans)]({{ roadmap_url }})"
  ],
  "h3_resource_layout": "Resource Layout",
  "p_resource_layout": "README and CHANGELOG are generated offline by `.python/generate_markdown.py` from the JSON sources above (standard library only, zero network). To change the docs, edit the JSON sources rather than the generated Markdown, then regenerate with the commands below; `--check` verifies that the outputs match the sources",
  "h3_links": "Links",
  "text_link_autojs6": "AutoJs6 project home",
  "text_link_ncnn": "Tencent NCNN",
  "text_link_ultralytics_export": "Ultralytics NCNN export guide",
  "text_link_third_party": "Third-party notices",
  "text_link_license": "License"
}
